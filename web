<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tienda Artesanal - Jabones y Velas</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      background: #f8f6f3;
      color: #333;
    }

    header {
      background: #e6d9c5;
      padding: 1rem;
      text-align: center;
    }

    header h1 {
      margin: 0;
      font-size: 2rem;
    }

    nav {
      display: flex;
      justify-content: center;
      gap: 2rem;
      margin-top: .5rem;
    }

    nav a {
      text-decoration: none;
      color: #333;
      font-weight: bold;
    }

    .hero {
      text-align: center;
      padding: 2rem;
      background: url("https://images.unsplash.com/photo-1612258263285-5b2fc54e07de") center/cover no-repeat;
      color: white;
    }

    .hero h2 {
      font-size: 2.5rem;
      margin: 0;
    }

    .container {
      max-width: 1100px;
      margin: auto;
      padding: 2rem;
    }

    .productos {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 2rem;
    }

    .producto {
      background: white;
      border-radius: 10px;
      padding: 1rem;
      text-align: center;
      box-shadow: 0 2px 6px rgba(0,0,0,.1);
      transition: transform .2s;
    }

    .producto:hover {
      transform: translateY(-5px);
    }

    .producto img {
      width: 100%;
      border-radius: 10px;
      max-height: 200px;
      object-fit: cover;
    }

    .producto h3 {
      margin: .5rem 0;
    }

    .precio {
      font-size: 1.2rem;
      color: #b17f4a;
      font-weight: bold;
    }

    button {
      background: #b17f4a;
      color: white;
      border: none;
      padding: .5rem 1rem;
      border-radius: 5px;
      cursor: pointer;
      margin-top: .5rem;
      transition: background .3s;
    }

    button:hover {
      background: #8e6437;
    }

    #carrito {
      margin-top: 3rem;
      background: white;
      padding: 1.5rem;
      border-radius: 10px;
      box-shadow: 0 2px 6px rgba(0,0,0,.1);
    }

    #carrito h2 {
      margin-top: 0;
    }

    .carrito-item {
      display: flex;
      justify-content: space-between;
      margin: .5rem 0;
      border-bottom: 1px solid #ddd;
      padding-bottom: .5rem;
    }

    .carrito-total {
      font-size: 1.2rem;
      font-weight: bold;
      margin-top: 1rem;
    }

    footer {
      background: #e6d9c5;
      text-align: center;
      padding: 1rem;
      margin-top: 2rem;
    }
  </style>
</head>
<body>

  <header>
    <h1>🌿 Tienda Artesanal 🌿</h1>
    <nav>
      <a href="#productos">Productos</a>
      <a href="#nosotros">Nosotros</a>
      <a href="#contacto">Contacto</a>
      <a href="#carrito">🛒 Carrito</a>
    </nav>
  </header>

  <section class="hero">
    <h2>Jabones y Velas Naturales</h2>
    <p>Hechos a mano con ingredientes ecológicos</p>
  </section>

  <main class="container">
    <h2 id="productos">Nuestros Productos</h2>
    <div class="productos">
      <div class="producto">
        <img src="https://images.unsplash.com/photo-1585238342022-5cfb3c7a511c" alt="Jabón artesanal">
        <h3>Jabón de Lavanda</h3>
        <p>Calma y relaja tu piel.</p>
        <p class="precio">€5,00</p>
        <button onclick="agregarCarrito('Jabón de Lavanda', 5)">Añadir al carrito</button>
      </div>

      <div class="producto">
        <img src="https://images.unsplash.com/photo-1599921521562-1cfa0a0ecb38" alt="Vela artesanal">
        <h3>Vela de Vainilla</h3>
        <p>Aroma dulce y acogedor.</p>
        <p class="precio">€8,00</p>
        <button onclick="agregarCarrito('Vela de Vainilla', 8)">Añadir al carrito</button>
      </div>

      <div class="producto">
        <img src="https://images.unsplash.com/photo-1600359756287-74f6d85c6e9c" alt="Vela artesanal">
        <h3>Vela de Canela</h3>
        <p>Perfecta para un ambiente cálido.</p>
        <p class="precio">€7,00</p>
        <button onclick="agregarCarrito('Vela de Canela', 7)">Añadir al carrito</button>
      </div>
    </div>

    <div id="carrito">
      <h2>🛒 Carrito de Compras</h2>
      <div id="carrito-items"></div>
      <p class="carrito-total">Total: €<span id="total">0</span></p>
      <!-- Aquí se cargará el botón de PayPal -->
      <div id="paypal-button-container"></div>
    </div>

    <h2 id="nosotros">Sobre Nosotros</h2>
    <p>En Tienda Artesanal elaboramos jabones y velas naturales con mucho amor. Cada producto está hecho a mano utilizando ingredientes sostenibles y respetuosos con el medio ambiente.</p>

    <h2 id="contacto">Contacto</h2>
    <p>📍 Dirección: Calle de la Naturaleza, 123, Madrid</p>
    <p>📧 Email: contacto@tiendaartesanal.com</p>
    <p>📞 Teléfono: +34 600 123 456</p>
  </main>

  <footer>
    <p>© 2025 Tienda Artesanal - Todos los derechos reservados</p>
  </footer>

  <!-- SDK de PayPal -->
  <script src="https://www.paypal.com/sdk/js?client-id=TU_CLIENT_ID_AQUI&currency=EUR"></script>

  <script>
    let carrito = [];

    function agregarCarrito(producto, precio) {
      carrito.push({ producto, precio });
      mostrarCarrito();
    }

    function eliminarItem(index) {
      carrito.splice(index, 1);
      mostrarCarrito();
    }

    function mostrarCarrito() {
      const contenedor = document.getElementById("carrito-items");
      contenedor.innerHTML = "";
      let total = 0;

      carrito.forEach((item, index) => {
        total += item.precio;
        contenedor.innerHTML += `
          <div class="carrito-item">
            <span>${item.producto} - €${item.precio}</span>
            <button onclick="eliminarItem(${index})">❌</button>
          </div>
        `;
      });

      document.getElementById("total").textContent = total;
      renderPayPalButton(total);
    }

    function renderPayPalButton(total) {
      document.getElementById("paypal-button-container").innerHTML = "";
      if (total > 0) {
        paypal.Buttons({
          createOrder: function(data, actions) {
            return actions.order.create({
              purchase_units: [{
                amount: { value: total.toFixed(2) }
              }]
            });
          },
          onApprove: function(data, actions) {
            return actions.order.capture().then(function(details) {
              alert("Gracias por tu compra, " + details.payer.name.given_name + " 🛍️");
              carrito = [];
              mostrarCarrito();
            });
          }
        }).render('#paypal-button-container');
      }
    }
  </script>

</body>
</html>
